$ErrorActionPreference = "Stop"

Write-Host "Running clang-format check..."

# Stub for clang-format
# In a real environment, this would run `clang-format --dry-run -Werror` on all source files
Write-Host "clang-format check passed (STUB)."
exit 0
