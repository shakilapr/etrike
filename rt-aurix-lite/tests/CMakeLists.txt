# RT-AURIX-Lite host tests. Owned by this subtree; included by
# native-test via add_subdirectory. One CTest invocation overall.

# test_* executables are added per commit (see work-plan.md vertical
# sequence). Keep test naming/sources local to this directory.

enable_testing()
